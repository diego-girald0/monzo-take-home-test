name: 'monzo_take_home_test'
version: '1.0.0'

profile: 'monzo_take_home_test'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"


models:
  monzo_take_home_test:
    +database: analytics-take-home-test
    +schema: DG_take_home_test
    sources:
      +materialized: ephemeral
    staging:
      +materialized: incremental
    core:
      +materialized: incremental

vars:
  project_prefix: 'DG'  # aliasing project models with initials