version: 2
sources:
    - name: monzo_datawarehouse
      schema: monzo_datawarehouse
      tables:
          - name: account_created
            description: Dataset logging when accounts were created.
          - name: account_closed
            description: Dataset logging when accounts were closed.
          - name: account_reopened
            description: Dataset logging when accounts were reopened.
          - name: account_transactions
            description: Dataset recording history of transactions by accounts.